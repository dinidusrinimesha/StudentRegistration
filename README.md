# 🧑‍🎓 Student Management System — Simple CRUD (PHP + MVC + OOP)

A lightweight **Student Management System** built using **pure PHP**, following the **MVC architecture** and **OOP concepts**.  
This project demonstrates a clean structure and full CRUD operations (Create, Read, Update, Delete) for managing student records.

---

## 🚀 Key Points

- Mainly focuses on CRUD operations   
- Built with **Pure PHP**  
- Implements **MVC (Model–View–Controller)** pattern  
- Uses **OOP principles** throughout  
- Database handled via **MySQL (mysqli OOP)**  

---

## 📥 Installation & Setup

Follow the steps below to set up this project on your local server:

### 1️⃣ Open your htdocs file

Open your htdocs and open the terminal in;

```bash
C:\xampp\htdocs
```

### 2️⃣ Clone the project in htdocs

Clone the project with the command;

```bash
git clone https://github.com/dinidusrinimesha/StudentRegistration.git
```

### 3️⃣ Open the project in Code Editor

Go to the project folder and open it with Visual Studio Code.

### 3️⃣ Run Database Initialization Scripts

In Visual Studio Code, open terminal and execute:

```bash
cd database\init
```

```bash
php createDb.php
```

```bash
php createTable.php
```

### 4️⃣ Start Your Local Server

Start your server(apache, mysql) and run the project.

---
